tests:
  drivers.uart.interrupt_api:
    tags:
      - uart
    filter: CONFIG_SERIAL_SUPPORT_INTERRUPT
    vendor_allow:
      - adi
      - sifli   

    harness: console  
    harness_config:  
      type: one_line  
      regex:          
        - "This is a FIFO test."